<h1>Projects Gallery</h1>
<% @projects.each do |project| %>
  <div>
    <h2><%= project.title %></h2>
    <p><%= project.description %></p>
    <% if project.url.present? %>
      <p><%= link_to 'View Project', project.url, target: '_blank' %></p>
    <% end %>
  </div>
<% end %>
