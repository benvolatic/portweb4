<nav class="bg-primary font-title text-white p-4 flex items-center relative" data-controller="dropdown">
  <!-- Main Dropdown Button -->
  <button 
  class="text-3xl mr-4 focus:outline-none" 
  aria-label="Toggle navigation menu" 
  data-action="click->dropdown#toggle"
>
  <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
    <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16m-7 6h7" />
  </svg>
</button>

  <!-- Main Dropdown Menu -->
  <ul 
    id="menu" 
    class="hidden absolute bg-secondary text-white text-xl rounded shadow-md p-4 space-y-2 top-16 left-4 w-48"
    data-dropdown-target="menu"
  >
    <li><%= link_to 'Home', root_path, class: 'block hover:underline' %></li>
    <li><%= link_to 'Projects', projects_path, class: 'block hover:underline' %></li>
    
    <!-- Extras Dropdown -->
    <li class="relative" data-controller="dropdown">
      <!-- Extras Button -->
      <button 
        class="block w-full text-left hover:underline focus:outline-none" 
        data-action="click->dropdown#toggle"
      >
        Extras
      </button>

      <!-- Nested Dropdown Menu -->
      <ul 
        class="hidden absolute bg-secondary text-white text-lg rounded shadow-md p-3 space-y-1 top-0 left-full ml-2 w-40"
        data-dropdown-target="menu"
      >
        <li><%= link_to 'Photos', photos_path, class: 'block hover:underline' %></li>
        <li><%= link_to 'Posts', posts_path, class: 'block hover:underline' %></li>
      </ul>
    </li>
  </ul>
</nav>